<% @posts.each do |post| %>
    <div class="row">
        <h2><%= post.title %></h2>
        <%= truncate(post.content, length: 300) %><br>
        <%= link_to "Read More", post_path(post) %>
   </div>
   <hr>
 <% end %>
